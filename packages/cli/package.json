{
  "name": "@styleconstitution/cli",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "bin": {
    "stylecon": "dist/stylecon.mjs"
  },
  "files": ["dist"],
  "scripts": {
    "build": "esbuild src/bin.ts --bundle --platform=node --target=node22 --format=esm --banner:js=\"#!/usr/bin/env node\" --outfile=dist/stylecon.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
