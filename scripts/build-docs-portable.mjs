import {mkdir,writeFile,copyFile} from 'node:fs/promises';import {loadCanonical} from './load-spec.mjs';
const b=await loadCanonical();const esc=s=>String(s).replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));
const cards=b.components.map(c=>`<article><h3>${esc(c.name)}</h3><p>${esc(c.purpose)}</p><code>${esc(c.id)}</code></article>`).join('');
const html=`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Style Constitution</title><style>body{font-family:system-ui;margin:0;background:#f8fafc;color:#0f172a}main{max-width:1100px;margin:auto;padding:48px 24px}header{display:grid;gap:16px}.hero{width:100%;border-radius:16px}section{margin-top:48px}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:16px}article{background:white;border:1px solid #e2e8f0;border-radius:12px;padding:20px}code{background:#eef2ff;padding:3px 6px;border-radius:5px}</style></head><body><main><header><h1>Astra + Fable Style Constitution</h1><p>Version ${esc(b.manifest.version)} · canonical rules for humans and agents.</p><img class="hero" src="architecture.png" alt="Astra and Fable Style Constitution architecture"></header><section><h2>Components</h2><div class="grid">${cards}</div></section><section><h2>Validation</h2><p>The published docs are derived from <code>/spec</code>; generated artifacts are never authoritative.</p></section></main></body></html>`;await mkdir('apps/docs/dist-portable',{recursive:true});await writeFile('apps/docs/dist-portable/index.html',html);await copyFile('architecture.png','apps/docs/dist-portable/architecture.png');console.log('portable docs built');
